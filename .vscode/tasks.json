{
    "version": "2.0.0",
    "tasks": [
        // Scrips base
        {
            "label": "mesclar base/last-release com a branch atual",
            "type": "shell",
            "problemMatcher": [],
            "windows": {
                "command": "if (git remote | findstr 'base') { git remote set-url base https://github.com/Felipe-Cavalca/base-repo.git } else { git remote add base https://github.com/Felipe-Cavalca/base-repo.git }; git fetch base; git merge base/latest-release"
            },
            "linux": {
                "command": "git remote | grep base && git remote set-url base https://github.com/Felipe-Cavalca/base-repo.git || git remote add base https://github.com/Felipe-Cavalca/base-repo.git; git fetch base && git merge base/latest-release"
            }
        },
        // Puxa APP
        {
            "label": "mesclar base/last-release com a branch atual",
            "type": "shell",
            "problemMatcher": [],
            "windows": {
                "command": "if (git remote | findstr 'base') { git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Front.git } else { git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Front.git }; git fetch base; git merge base/latest-release"
            },
            "linux": {
                "command": "git remote | grep base && git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Front.git || git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Front.git; git fetch base && git merge base/latest-release"
            }
        },
        // Puxa API
        {
            "label": "mesclar base/last-release com a branch atual",
            "type": "shell",
            "problemMatcher": [],
            "windows": {
                "command": "if (git remote | findstr 'base') { git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Back.git } else { git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Back.git }; git fetch base; git merge base/latest-release"
            },
            "linux": {
                "command": "git remote | grep base && git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Back.git || git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Back.git; git fetch base && git merge base/latest-release"
            }
        },
        // Puxa database
        {
            "label": "mesclar base/last-release com a branch atual",
            "type": "shell",
            "problemMatcher": [],
            "windows": {
                "command": "if (git remote | findstr 'base') { git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Database.git } else { git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Database.git }; git fetch base; git merge base/latest-release"
            },
            "linux": {
                "command": "git remote | grep base && git remote set-url base https://github.com/Felipe-Cavalca/BifrostPHP-Database.git || git remote add base https://github.com/Felipe-Cavalca/BifrostPHP-Database.git; git fetch base && git merge base/latest-release"
            }
        }
    ]
}
